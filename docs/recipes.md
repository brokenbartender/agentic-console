# Recipes

## Codebase summary

```
python runtime\run.py run "summarize the architecture" --two-agent
```

## Memory-first retrieval

```
python runtime\run.py memory search "deployment"
```

## Workflow-driven task

```
python runtime\run.py workflow run workflows\sample.json --goal "ship status update"
```
